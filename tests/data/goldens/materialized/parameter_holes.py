def run(session, limit: int=5, *items, debug=False, **options):
    value = session
    session__astichi_scoped_1 = 'local'
    local_session = session__astichi_scoped_1
    return session

async def async_run(token):
    return token

def foo(before, user_param, after):
    user_code(user_param)
    return (before, user_param, after)

def keyword_only(*, existing=False, inserted=True):
    return (existing, inserted)

def optional_annotation(timeout=10):
    return timeout
